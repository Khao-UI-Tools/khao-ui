import { Meta, IconGallery, IconItem } from "@storybook/addon-docs/blocks";
import Icon from "../../src/icons/Icon.svelte";
import { iconNames } from "../../src/icons/types/IconName";
import { version } from "../../package.json";

<Meta title="Icons/Icon Library" />

# Icon Library

## (Khao UI v{version})

Icons are used to represent actions, objects, or concepts. Most of the icons come from [Material Icons](https://fonts.google.com/icons) and [Icomoon.io](https://icomoon.io/)

<IconGallery>
  {iconNames.map(iconName => (
    <IconItem name={iconName} key={iconName}>
      <khao-icon iconName={iconName} sizeFactor="8" />
    </IconItem>
  ))}
</IconGallery>
